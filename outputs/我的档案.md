# 我的档案

生成时间：2026-03-19 11:58 CST
主题：我的档案

## 基本画像

- 当前主要使用中文交流。
- 日常作息已记录：起床约 `07:40`，睡觉约 `24:00`。
- 日程、提醒、计划整理默认交给 `life-assistant` 处理。
- 涉及日历同步时，默认使用 Apple 生态（日历 / 提醒事项）。

## 当前关注重点

- 正在持续搭建和推进 OpenClaw 的能力包、Skilltree、SOP 依赖联动与多代理编排体系。
- 非常重视把工作从“能做”推进到“可编排、可恢复、可复用、可追踪”。
- 已确认并要求推进的五条主线任务：
  - 自动发自媒体内容系统
  - 毕业论文助手
  - 猫贩子信息收集与风险评估平台
  - 短视频出镜与热点模仿策略助手
  - 英语提升系统

## 偏好与工作方式

- 搜索默认优先使用 `web-search-plus`。
- 当你说“用这个”时，默认直接安装并集成对应工具或仓库。
- 多子任务、并行开发、分工收尾类任务，默认优先按 `subagent-driven-development` 方式完成。
- 你更偏好系统化、长期可复用的做法，而不是一次性临时处理。
- 你明确要求：凡是你亲自定下的规则，需要长期记住并持续执行。

## 内容与表达偏好

- 内容交付默认要给完整可发布包：标题、正文、Tags、配图建议。
- 文案偏好“小盖风格厚版”：有叙事、有主线、有厚度，不喜欢空泛、列表式、机械感太强的内容。
- 当需要多内容时，偏好分条逐个完整交付，而不是缩水合并。
- 内容创作时，案例优先用真实、熟悉、可感知的案例，避免虚构。
- 内容生成上，你明确要求：一次只生成一条；主题需要先发散再选择。

## 已固化的重要规则

- 提醒消息默认由 `life-assistant` 发送，并带前缀 `【助理】`；主会话只转述指令。
- 小红书评论语义识别默认要拆给多个 session 处理，每个 session 不超过 50 条；不允许默认先走本地 heuristic 兜底。
- 小红书评论抓取 SOP 必须全领域通用，只有明确指定时才切换到领域专用字段。
- 评论抽取结果表头默认使用具体字段，如：`国家`、`品牌`、`产品`。
- 默认模型统一设为 `yunyi-codex/gpt-5.4`。

## 当前档案中仍待补充的信息

- 真实姓名：未明确记录
- 常用称呼：未明确记录
- 时区：运行环境为 `Asia/Shanghai`，但 `USER.md` 尚未正式补全
- 更完整的长期背景、项目优先级与生活偏好，仍可继续补充到正式档案中

## 一句话总结

你当前的画像更像是一个强系统导向、重执行闭环、重长期复用的人：既要把事情做出来，也要把方法、流程和代理分工沉淀成以后能持续复用的体系。