{
  "name": "pr",
  "required_checks": ["branch_pushed", "pr_created", "ci_passed", "verified"],
  "notes": [
    "PR existence alone is not done.",
    "Branch must be pushable and CI must pass before notify."
  ]
}
