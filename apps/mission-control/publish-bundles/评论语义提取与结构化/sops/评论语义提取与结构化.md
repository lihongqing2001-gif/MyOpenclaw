# SOP: 评论语义提取与结构化

- Capability ID: cap.openclaw.sop.xiaohongshu-comment-semantic-extraction-auto-excel
- Node ID: sop-xiaohongshu-comment-semantic-extraction-auto-excel
- Domain: 社交媒体与内容
- Area: 内容采集

## Summary
For a given Xiaohongshu note link, fetch the full comment area, normalize comment semantics into a single business-ready Excel, and default to this SOP whenever the user asks to crawl a Xiaohongshu link's comments.

## Preconditions
Chrome is logged in to Xiaohongshu；`xiaohongshu-skills` CLI is available；Output path is writable；Do not require `openpyxl`; if unavailable, still produce a valid `.xlsx`

## Inputs
- Xiaohongshu note URL (required)
- Output path (optional) (optional)
- Batch size (optional) (optional)

## Commands
- `python3 /Users/liumobei/.openclaw/workspace/apps/mission-control/scripts/run_xhs_comment_semantic_extract.py --note-url <Xiaohongshu note URL> --output <Output path (optional)> --batch-size <Batch size (optional)>`

## Required Capabilities
- `cap.openclaw.skill.journal-revision-workflow` · journal-revision-workflow · bundled
- `cap.openclaw.skill.self-improving` · Self-Improving Agent (With Self-Reflection) · bundled
- `cap.openclaw.skill.alex-session-wrap-up` · session-wrap-up · bundled
- `cap.openclaw.skill.social-content` · social-content · bundled
- `cap.openclaw.skill.xiaohongshu-skills` · xiaohongshu-skills · bundled

## Use Cases
- "爬这个链接评论区": For a given Xiaohongshu note link, fetch the full comment area, normalize comment semantics into a single business-ready Excel, and default to this SOP whenever the user asks to crawl a Xiaohongshu link's comments.
- "抓取这个小红书评论": For a given Xiaohongshu note link, fetch the full comment area, normalize comment semantics into a single business-ready Excel, and default to this SOP whenever the user asks to crawl a Xiaohongshu link's comments.
- "把这个链接评论整理出来": For a given Xiaohongshu note link, fetch the full comment area, normalize comment semantics into a single business-ready Excel, and default to this SOP whenever the user asks to crawl a Xiaohongshu link's comments.
- "导出这个笔记评论到 Excel": For a given Xiaohongshu note link, fetch the full comment area, normalize comment semantics into a single business-ready Excel, and default to this SOP whenever the user asks to crawl a Xiaohongshu link's comments.
