# 2026-03-09

- User mainline task: design a system integrating skill library, knowledge base, agent architecture, task publishing/status, and a 3-level skill tree (domain → sub-area → SOP). Each SOP has a page with full info and prerequisites so new OpenClaw installs can run end-to-end. App architecture v1.2 exists but many features incomplete.
- SOP to remember: Xiaohongshu note capture workflow using xiaohongshu-skills CLI. Entry point: `/Users/liumobei/.openclaw/workspace/skills/xiaohongshu-skills/scripts/cli.py` with `get-feed-detail` and `--load-all-comments` then export to Excel on Desktop.
- New SOP (semantic extraction + valid/invalid tagging): `/Users/liumobei/.openclaw/workspace/sops/xhs_comment_semantic_extract.md`.

## 20:45 - XHS SOP + Skilltree
- Updated SOP to store temp files per feed under sops/xhs_comment_semantic_extract_projects/<feed_id>/ and added cleanup step; committed 59d67c6.
- Archived legacy tmp_xhs_* files into per-feed project folders; removed root duplicates.
- Rebuilt XHS Excel outputs with correct batch mapping and content validation; outputs at Desktop xhs_note_<feed_id>_ai.xlsx.
- Started Openclaw Agent 2.1 server and resolved port conflicts.
- Modified /Users/liumobei/code/openclaw/skillstree/2.1/Openclaw-Agent--main/server.ts to load skilltree data from /Users/liumobei/.openclaw/workspace/content_system/skilltree/data.json for /api/v1/skill-tree.

## 主线任务固化标准
- 主线任务命名：ML-YYYYMMDD-<主题>-<阶段>
- 需要可索引进度与内容；作为固定主线任务执行规范。
- 当前主线：OpenClaw 能力包 + Skilltree 面板自动化握手与SOP依赖联动框架

## 规则
- 不使用 memory_store 工具；记忆与规则改用 qmd 体系/文件记录。

## 22:18 - OpenClaw framework mainline
- User selected capability scheme C: name cap.<org>.<domain>.<name>, publish via ClawHub/registry.
- Mainline task created: ML-20260309-OpenClaw-Framework-Stage1 with v0.1 specs in agents/specs/openclaw-framework/.
- User instruction: stop using memory_store tool; use qmd/files for memory.

## Preference
- When user asks to open a local URL on phone, default to providing a temporary public link (tunnel), unless told otherwise.

## UI Plan (Skilltree Fusion)
- Center agent avatar; radial categories -> domains -> SOP nodes.
- SOP node binds capability package (skills/knowledge/outputs), one-click deploy + run.
- Right panel shows package details, dependencies, status, outputs.
- States: locked (missing deps), lit (installed/healthy), running (progress), failed (fix tips).
