# 2026-03-18

- 更新 `sops/xhs_comment_semantic_extract.md`：以后用户让抓取/爬取小红书链接评论区时，默认自动调用该 SOP。
- 默认交付为桌面单个 Excel：`xhs_note_<feed_id>_comments.xlsx`。
- SOP 已补充本次经验：必须全量抓取评论（`--load-all-comments`）、需要语义标准化、要拆分多商品评论、优先输出中文表头、避免只做原始导出。
- 用户新增默认规则：小红书评论语义识别默认分发给多个 session 处理，每个 session 处理不超过 50 条评论。
- 用户纠正：小红书评论抓取 SOP 必须是全领域通用，不能默认限制在酒类或其他单一垂类；只有用户明确指定时才切到领域专用字段。
- 用户明确要求：最终表头不要“实体/领域/关键信息”这类抽象字段，默认只要具体结果字段：`国家`、`品牌`、`产品`。
- 用户新增硬规则：小红书评论语义识别默认必须拆给多个 session 做，不允许先走本地规则兜底；本地 heuristic 只能在用户明确允许时使用。
- 用户要求：默认模型统一设置为 `yunyi-codex/gpt-5.4`。
- 用户要求：小红书评论项目支持历史对比、标出新增评论，并纳入项目管理/链接跟踪；默认放在项目文件夹中，不堆在桌面，后续可主动更新。
